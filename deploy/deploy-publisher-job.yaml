apiVersion: batch/v1
kind: Job
metadata:
  labels:
    run: publisher
  name: publisher
spec:
  template:
    metadata:
      labels:
        run: publisher
    spec:
      containers:
      - image: felipecruz/rabbitmq-publisher
        imagePullPolicy: Always
        name: publisher
        resources: {}
      restartPolicy: OnFailure
status: {}
